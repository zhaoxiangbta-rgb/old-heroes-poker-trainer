# 移动端写实牌桌设计校验

## 对照范围

- 设计基准：已确认的 Image 2 写实牌桌样图。
- 实现截图：`test-results/mobile-portrait.png`、`mobile-compact-portrait.png`、`mobile-landscape.png`。
- 视口：430×932、430×760、932×430。

## 校验结果

- 视觉层级：通过。底池、公共牌、玩家、手牌和操作区顺序明确。
- 材质表现：通过。深绿桌布、黑色皮革、黄铜描边和实体筹码按钮形成统一写实风格。
- 头像：通过。六个虚构人物照片使用独立本地资源，圆形裁切完整，头像与余码、下注标签分离。
- 牌面：通过。手牌和公共牌使用高对比象牙白牌纸；点数和花色独立放大，红黑花色清晰。
- 操作区：通过。纵向推杆已替换为黑皮革与黄铜质感的横向推杆；`最低 / ½ / ⅔ / 1× / ALL IN` 直接融入轨道并支持吸附，重合金额自动合并。任意合法金额仍可连续选择，不会拖动即提交。
- 三栏平衡：通过。下层左侧为余码与位置，中间为大点数、大花色手牌，右侧为规则引擎允许的弃/过/跟或加注。三区不重叠，按钮触摸目标满足 44 px 下限。
- 空间占用：通过。430 px 宽竖屏操作区为 142–146 px，比原纵杆布局减少至少 50 px；横屏在右侧独立区域内保持同一操作模型。
- 遮挡与适配：通过。三种视口均无横向溢出；操作区不覆盖牌桌，推杆不遮挡公共牌，玩家信息彼此不重叠。
- 离线性：通过。六个头像、两张纹理和三张筹码图均进入 PWA 预缓存，无远程图片地址。

## 剩余 P3 微调

- 对手未公开底牌仍沿用现有牌背图案，后续可单独生成更写实的品牌牌背。
- 紧凑横屏为保证可操作性压缩了辅助文字尺寸，手牌、按钮和轨道触摸区未缩小。

final result: passed
